<%= react_component("TicketDash", {uid: @user.id, 
                                  tid: @ticket.id, 
                                  ticketTitle: @ticket.title,
                                  trialMode: true,
                                  pid: @ticket.project_id }) %>