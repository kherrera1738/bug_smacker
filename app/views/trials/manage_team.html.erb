<%= react_component("TeamManageDash/index", { uid: @user.id, 
                                              projID: @project.id, 
                                              orgID: @project.organization_id,
                                              trialMode: true }) %>