<%= provide(:title, "Trial Project Dashboard")%>

<%= react_component("ProjectDash/index", { uid: @user.id, 
                                          pID: @project.id, 
                                          projName: @project.name,
                                          trialMode: true }) %>